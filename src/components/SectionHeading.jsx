import React from 'react'

const SectionHeading = ({heading}) => {
  return (
    <div className='mt-10 py-2 px-4 text-lg bg-gray-900 font-semibold text-white'>{heading}</div>
  )
}

export default SectionHeading